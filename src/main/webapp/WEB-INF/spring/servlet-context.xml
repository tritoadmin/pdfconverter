<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:aop="http://www.springframework.org/schema/aop"
    xmlns:mvc="http://www.springframework.org/schema/mvc"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    					http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
                        http://www.springframework.org/schema/context
                        http://www.springframework.org/schema/context/spring-context-3.2.xsd
                        http://www.springframework.org/schema/aop
                        http://www.springframework.org/schema/aop/spring-aop-3.2.xsd
                        http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/mvc
                        http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd"
                        >

	<bean id="dataSource"
	      class="org.springframework.jdbc.datasource.DriverManagerDataSource">
	    <property name="driverClassName" value="oracle.jdbc.driver.OracleDriver"/>
	    <property name="url" value="jdbc:oracle:thin:@www.trito.co.kr:1521:TRITODB"/>
	    <property name="username" value="trito_dev"/>
	    <property name="password" value="trito114"/>
	</bean>

	<beans:bean name="template" class="org.springframework.jdbc.core.JdbcTemplate">
		<beans:property name="dataSource" ref="dataSource" />
	</beans:bean>

	<!-- 어노테이션 설정을 사용할 수 있도록 해주는 태그  -->
    <context:annotation-config/>

    <!-- bean 자동생성 패키지 등록  -->
    <context:component-scan base-package="kr.co.trito"/>

    <mvc:annotation-driven/>

    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="104857600"/>
    </bean>

    <context:property-placeholder location="classpath:/config.properties"/>

    <!-- <mvc:interceptors>
	    <mvc:interceptor>
	        <mvc:mapping path="/convert/**"/>
	        <bean class="kr.co.trito.pdf.interceptor.HmacAuthInterceptor"/>
	    </mvc:interceptor>
	</mvc:interceptors> -->

	<!-- 1. Interceptor 클래스 빈 등록 -->
    <bean id="hmacInterceptor" class="kr.co.trito.pdf.interceptor.HmacAuthInterceptor" />

    <!-- 2. Interceptor 설정 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!-- 2-1. 적용할 URL 패턴 (전체) -->
            <mvc:mapping path="/**"/>

            <!-- 2-2. 제외할 URL 패턴 (excludePathPatterns) -->
            <mvc:exclude-mapping path="/css/**"/>
            <mvc:exclude-mapping path="/js/**"/>
            <mvc:exclude-mapping path="/images/**"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/convert/token.do"/>

            <!-- 2-3. 인터셉터 빈 연결 -->
            <ref bean="hmacInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>



</beans>
